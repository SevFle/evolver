Fix the following issues in the codebase at /workspace:

Test failures to resolve:

> hookrelay@0.1.0 test
> vitest run

sh: 1: vitest: not found

Additional context: 1) Create src/server/db/schema/index.ts with the complete database schema (users, endpoints, events, deliveries, teams tables with proper relations) that was supposed to exist. 2) Update drizzle.config.ts to use DATABASE_URL_NON_POOLING for migrations. 3) Remove or rename the rawSql export to prevent SQL injection. 4) Verify the app starts with `pnpm build` and fix the vitest issue by ensuring dev dependencies are installed.

RULES:
- Fix ALL issues mentioned above
- Do not introduce new bugs
- Keep existing tests passing
- Write tests for the fixes if not covered
- Run tests after fixing to verify
